# PrimeNG 21 still declares peer @angular/* ^21 while this app is on Angular 22.
# The combination is validated (build + tests green); allow npm (incl. `npm ci` in Docker)
# to install against the resolved lockfile without failing on the peer-dependency range.
legacy-peer-deps=true
